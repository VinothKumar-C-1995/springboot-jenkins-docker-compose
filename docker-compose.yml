version: "3.8"
services:
  app:
    build: .
    container_name: springboot-app
    ports:
      - "8080:8080"
    volumes:
      - app_data:/app/data
      - app_logs:/app/logs
    restart: always
volumes:
  app_data:
  app_logs:
