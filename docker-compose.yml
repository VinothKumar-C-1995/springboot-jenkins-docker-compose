version: "3.9"

services:
  web1:
    image: nginx:latest
    container_name: nginx1
    ports:
      - "8081:80"
    restart: unless-stopped

  web2:
    image: nginx:latest
    container_name: nginx2
    ports:
      - "8082:80"
    restart: unless-stopped
